# نظام التداول الآلي للعملات الرقمية

نظام تداول آلي متقدم للعملات الرقمية يعتمد على اكتشاف Order Blocks وتنفيذ الأوامر تلقائياً مع إرسال تنبيهات وصور الرسم البياني عبر Telegram.

## المميزات الجديدة

- **📊 رسوم بيانية تلقائية**: إرسال صور الرسم البياني عند اكتشاف Order Block
- **🔔 تنبيهات متقدمة**: رسائل نصية وصور عبر Telegram
- **📈 مخططات شموع يابانية**: عرض واضح لحركة الأسعار مع تمييز Order Blocks
- **⚡ ping/pong محسن**: ping كل 20 ثانية لضمان استقرار الاتصال

## التقنيات المستخدمة

- **Python 3.12**: اللغة الأساسية
- **MongoDB**: تخزين البيانات التاريخية والـ Order Blocks
- **Redis**: التخزين المؤقت والأحداث
- **Docker**: حاويات الخدمات
- **WebSocket**: البيانات المباشرة من البورصة
- **Matplotlib & mplfinance**: إنشاء الرسوم البيانية
- **Telegram Bot API**: التنبيهات والصور

## التثبيت والتشغيل

### 1. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 2. إعداد متغيرات البيئة

```bash
# انسخ ملف الإعدادات
cp .env.example .env

# عدّل المفاتيح في ملف .env
nano .env
```

### 3. تشغيل النظام

```bash
# تشغيل جميع الخدمات
docker-compose up -d

# مراقبة السجلات
docker-compose logs -f
```

### 4. تشغيل المكونات منفصلة

```bash
# استهلاك الشموع
python3 WebSocket_Candle.py

# محرك الاستراتيجية (في terminal منفصل)
python3 Strategy_Engine.py
```

## هيكل المشروع

```
trading_system/
├── WebSocket_Candle.py    # استهلاك الشموع
├── Strategy_Engine.py     # محرك الاستراتيجية + إرسال الرسوم
├── Order_Block.py         # اكتشاف Order Blocks
├── chart_generator.py     # إنشاء الرسوم البيانية
├── telegram_bot.py        # تنبيهات Telegram
├── Database.py            # اتصالات قاعدة البيانات
├── symbols.py             # رموز العملات
├── ticker_rules.py        # قواعد التداول
└── docker-compose.yml     # تكوين Docker
```

## الميزات الجديدة

### 📊 الرسوم البيانية التلقائية

عند اكتشاف Order Block، يتم:
1. إنشاء مخطط شموع يابانية للعملة
2. تمييز منطقة Order Block بالألوان
3. إرسال الصورة إلى Telegram مع تفاصيل Order Block

### 🔔 التنبيهات المحسنة

- رسائل نصية مفصلة مع تفاصيل Order Block
- صور الرسم البياني مع التسميات التوضيحية
- حذف تلقائي للصور بعد الإرسال

### ⚡ استقرار الاتصال

- ping كل 20 ثانية لضمان استقرار WebSocket
- إعادة اتصال تلقائية في حالة الانقطاع
- معالجة أخطاء الاتصال

## الأمان

⚠️ **تحذير مهم**: جميع المفاتيح في الملفات مكشوفة ويجب تدويرها فوراً!

1. سجل دخول إلى حساباتك في البورصات
2. احذف المفاتيح القديمة
3. أنشئ مفاتيح جديدة
4. حدث ملف `.env` بالمفاتيح الجديدة

## المراقبة

- **MongoDB**: `localhost:27017`
- **Redis**: `localhost:6379`
- **السجلات**: `docker-compose logs -f`
- **الرسوم**: يتم حفظها مؤقتاً ثم حذفها بعد الإرسال

## المساهمة

1. Fork المشروع
2. أنشئ فرع للميزة الجديدة
3. اعمل Commit للتغييرات
4. ادفع الفرع
5. أنشئ Pull Request

## الترخيص

هذا المشروع مخصص للاستخدام التعليمي. استخدمه على مسؤوليتك الخاصة.
